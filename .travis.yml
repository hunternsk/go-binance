go_import_path: github.com/betazoid/go-binance

language: go

go:
  - "1.10.x"

before_install:
  - go get -t -v ./...

script:
  - go test -v ./... -race -coverprofile=coverage.txt -covermode=atomic

after_success:
  - bash <(curl -s https://codecov.io/bash)
